<x-seguimiento-layout><div class="mx-auto max-w-5xl"><div class="mb-5 flex justify-between"><div><h2 class="text-2xl font-semibold text-gray-800">Detalle presupuestario</h2><p class="text-sm text-gray-500">{{ $presupuesto->proyecto->codigo }} - {{ $presupuesto->proyecto->nombre }}</p></div><div class="flex gap-2"><a href="{{ route('presupuestos.listar') }}" class="rounded-md border px-4 py-2 text-sm">Volver</a>@if($presupuesto->estado==='Borrador' && puedeHacer('presupuestos','editar'))<a href="{{ route('presupuestos.edit',$presupuesto) }}" class="rounded-md bg-blue-600 px-4 py-2 text-sm text-white">Editar</a>@endif</div></div>
<div class="grid gap-4 md:grid-cols-4">@foreach(['Aprobado'=>number_format((float)$presupuesto->proyecto->presupuesto_aprobado,2),'Programado'=>number_format((float)$presupuesto->monto_programado,2),'Vigente'=>number_format((float)$presupuesto->presupuesto_vigente,2),'Ejecutado'=>number_format((float)$presupuesto->monto_ejecutado,2)] as $k=>$v)<div class="rounded-lg border bg-white p-4"><p class="text-xs uppercase text-gray-500">{{ $k }}</p><p class="mt-1 font-semibold">USD {{ $v }}</p></div>@endforeach</div>
<div class="mt-4 grid gap-4 md:grid-cols-3"><div class="rounded-lg border bg-white p-5 md:col-span-2"><p><strong>Periodo:</strong> {{ $presupuesto->periodo }} de {{ $presupuesto->anio }}</p><p class="mt-2"><strong>Fecha de corte:</strong> {{ $presupuesto->fecha_corte->format('d/m/Y') }}</p><p class="mt-2"><strong>Estado:</strong> {{ $presupuesto->estado }}</p><h3 class="mt-4 font-semibold">Observaciones</h3><p class="mt-1 whitespace-pre-line text-sm text-gray-700">{{ $presupuesto->observaciones ?: 'No registra.' }}</p><p class="mt-5 text-xs text-gray-500">Registrado por {{ $presupuesto->creador?->nombres }} {{ $presupuesto->creador?->apellidos }} el {{ $presupuesto->created_at->format('d/m/Y H:i') }}</p></div><div class="rounded-lg border bg-white p-5"><p class="text-xs uppercase text-gray-500">Ejecución</p><p class="mt-1 text-3xl font-semibold">{{ number_format($presupuesto->porcentaje_ejecucion,2) }}%</p><p class="text-sm">{{ $presupuesto->alerta }}</p></div></div></div></x-seguimiento-layout>
